github:
  token: $(gh auth token)
  repos:
    - owner: bketelsen
      repo: oneagent
      labels: [dogfood]

agent:
  provider: claude-code
  stallTimeout: 300000
  maxRetries: 1

concurrency:
  max: 1

poll:
  interval: 30000

web:
  port: 3000
  enabled: true
